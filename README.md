# careroute

An AI intake triage agent built on the Anthropic API with tool calling.

## Setup

```bash
pip install -r requirements.txt
cp .env.example .env
```

Add your `ANTHROPIC_API_KEY` to `.env`.

## Status

Early scaffolding. See [PLAN.md](PLAN.md) for the build phases.
